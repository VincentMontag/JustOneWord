import { Container, Typography } from "@mui/material";

function Game() {
    return (
        <Container>
            <Typography variant="h4">Spiel läuft...</Typography>
            {/* TODO GAME CONTENT */}
        </Container>
    );
}

export default Game;
